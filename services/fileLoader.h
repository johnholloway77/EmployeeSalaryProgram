#ifndef FILELOADER_H
#define FILELOADER_H

char loadEmployees(char *file_address);
char validFileName(char *file_address);

// dont need to include the source file of the same name. will cause error!
//#include "fileLoader.c"

#endif // FILELOADER_H
